TODO:

+ The return message duo to the SQL validation violation errors is Internal server Error!! (Try to store another committee with the same name)

+ Middleware to detcet if the logged in user is an upper board that gives the privilage assigned to it to the same authority level or higher.

+ Middleware to detcet if the logged in user is a high board that gives the privilage assigned to it to the same authority level or higher.

+ Middleware to detcet if the logged in user is a member that gives the privilage assigned to it to the same authority level or higher.
